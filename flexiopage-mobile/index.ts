import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent appelle AppRegistry.registerComponent('main', () => App)
// et configure correctement l'environnement (Expo Go ou build natif).
registerRootComponent(App);
