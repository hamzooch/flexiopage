NEXT_PUBLIC_API_URL=http://localhost:5050

# Local subdomain testing via lvh.me (resolves *.lvh.me → 127.0.0.1).
# With this set, "View store" buttons emit http://<slug>.lvh.me:3002
# and Next middleware reads the Host to rewrite to /store/<slug>/…
NEXT_PUBLIC_STOREFRONT_DOMAIN=lvh.me:3002
